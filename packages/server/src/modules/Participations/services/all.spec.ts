describe('services', () => {
  // user management
  it('should be able to create a new user', () => {})
  it('should be able sign in', () => {})
  it('should redirect to users page', () => {})
  it('should redirect to admin page', () => {})
  // questions management
  it('should be able to create a new question as admin', () => {})
  it('should be able to list questions as admin', () => {})
  it('should be able to list answers by question', () => {})
  it('should be able to list anserws by user', () => {})
  // answers management
  it('should be able to add new anser to a question as user', () => {})
  it('should be able to list answers as user', () => {})
})